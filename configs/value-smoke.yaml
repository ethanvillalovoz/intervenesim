seed: 83
train_policy_seeds: [27]
eval_policy_seeds: [127]
tasks: [can]
disturbances: [nominal, gripper_slip]
candidate_steps: [20, 60]
target_budgets: [0.25, 0.50]
train_episodes: 1
eval_episodes: 1
value_epochs: 2
value_hidden_dims: [32, 32]
batch_size: 64
learning_rate: 0.001
weight_decay: 0.000001
max_steps: 180
device: auto
research_run: artifacts/runs/research-v1
output_dir: artifacts/runs/value-smoke
